---
- name: install packages
  become: yes
  package:
    name: "{{ item }}"
    state: present
  loop: "{{ packages }}"

- name: install snaps
  become: yes
  snap:
    name: "{{ name }}"
    channel: "{{ channel }}"
  loop: "{{ snaps }}"
